import BaseSpacing from "@/components/base";

const NotFound = () => {
  return (
    <BaseSpacing className="flex flex-col items-center justify-center w-full h-full">
      <div>Not found</div>
    </BaseSpacing>
  );
};

export default NotFound;
